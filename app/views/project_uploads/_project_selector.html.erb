<%= render "shared/project_selector",
      companies: local_assigns.fetch(:companies),
      company: local_assigns.fetch(:company),
      project: local_assigns.fetch(:project),
      select_id: "scope-project-select",
      empty_message: "No projects yet. Create one to upload scope data.",
      path_for: ->(co, proj) { company_project_upload_path(co, proj) } %>
